"use client";

import Scene from "@/components/scene/Scene";

/**
 * Bosh sahifa — endi 3D Command Deck sahnasini ko'rsatadi.
 */
export default function Home() {
  return (
    <main className="w-full h-full bg-[var(--color-bg-primary)]">
      <Scene />
    </main>
  );
}